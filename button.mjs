

function findsum(arr){
    let sum=0
    for(a=0; a<arr.lenght; a++){
        sum +=arr[a]
    }
    document.write(`The array is ${arr} <br>
    The sum of all the numbers in the array is ${sum}`)
}
    
//actions can speak more louder than words

